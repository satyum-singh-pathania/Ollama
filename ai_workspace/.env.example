# Copy this file to `.env` (which is gitignored) and adjust as needed.
# Every value here overrides the defaults in app/config.py.

OLLAMA_BASE_URL=http://localhost:11434
DEFAULT_MODEL=mistral
CONNECT_TIMEOUT=5
READ_TIMEOUT=120
